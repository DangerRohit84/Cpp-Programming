#include<bits/stdc++.h>
using namespace std;

int main() {
    array<int, 5> num = {1, 2, 3, 4, 5};
    map<int, int> freq;
    for (int n : num) {
        freq[n]++;
    }
    for (
        
    ) {
        cout << pair.first << " : " << pair.second << endl;
    }
    return 0;
}
